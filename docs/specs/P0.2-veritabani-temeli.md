# P0.2 — Veritabanı Temeli · Spec (Ne yapıldı)

## Migration

`supabase/migrations/20260725121441_p0_2_db_foundation.sql`

## Enum tipleri

| Tip | Değerler |
|---|---|
| `audit_action` | `insert, update, delete, restore` |
| `audit_source` | `user, system, automation, migration, import` |
| `file_category` | `document, image, avatar, export, other` |

## Fonksiyonlar

| Fonksiyon | Dönüş | Güvenlik | Not |
|---|---|---|---|
| `touch_updated_at()` | `trigger` | invoker | BEFORE UPDATE: `updated_at := now()` |
| `is_active_user()` | `boolean` | definer, `search_path=''` | `auth.uid()` public.users'da aktif & silinmemiş mi |
| `has_permission(text)` | `boolean` | definer, `search_path=''` | Faz 0: `is_active_user()` döner (P0.5'te dolacak) |

`execute` yetkisi: `is_active_user`, `has_permission` → `anon, authenticated`.

## Uygulama

- Hedef: cloud proje **CRM** (`kkxvoxeqfsaqzklrtgrw`, eu-west-1, Postgres 17).
- `supabase link` + `supabase db push` → başarılı.
- `supabase migration list`: yerel `20260725121441` = uzak `20260725121441` (senkron).

## Doğrulama (psql, uzak DB)

- 3 enum ve değerleri birebir doğrulandı.
- 3 fonksiyon; dönüş tipleri ve `security definer` bayrağı doğrulandı.

## Sonraki paketlere devir

- `current_app_user()` P0.4'te (users satır tipi orada oluşur).
- `has_permission()` gerçek gövdesi P0.5'te.
- P0.3 audit trigger'ı aktör kimliğini `auth.uid()` / `auth.users` üzerinden alacak
  (public.users'a bağımlı olmadan).
