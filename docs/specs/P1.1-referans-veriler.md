# P1.1 — Referans Veriler (Satış Tanımları) · Spec

## Migration

`supabase/migrations/20260726090000_p1_1_reference_data.sql`

## Tablolar (hepsi ayarlardan yönetilir)

Ortak sütunlar: `id, key(unique), label, sort_order, color, is_active, is_system, ts`.

| Tablo | Ek | Tohum |
|---|---|---|
| `interaction_channels` | — | telefon, whatsapp, eposta, instagram, telegram, online_toplanti, ziyaret (7) |
| `interaction_outcomes` | `is_positive` | ulasildi(+), ulasilamadi, sonra_aranacak, teklif_isteniyor(+), numune_isteniyor(+), siparis_olustu(+), olumsuz (7) |
| `lead_sources` | — | web_scraper, referans, manuel, fuar, web_sitesi, sosyal_medya, reklam, diger (8) |
| `lead_statuses` | `is_closed` | yeni, temas_kuruldu, ilgileniyor, ulasilamiyor, olumsuz(kapalı), donusturuldu(kapalı, sistem) (6) |
| `customer_types` | — | yurtici, ihracat (2) |
| `tags` | color | premium, ihracat, yuksek_potansiyel, vip, riskli, tek_seferlik (6) |
| `entity_tags` | (entity_type, entity_id, tag_id) | çoktan çoğa |

Tohumlanan tüm satırlar `is_system=true`.

## Trigger'lar

- Her tanım tablosu: `touch_updated_at`, `guard_system_reference` (is_system'de
  key/is_system değişmez), `prevent_reference_delete` (fiziksel silme yok), `audit_trigger`.
- `entity_tags`: `audit_trigger` (etiket ekleme/çıkarma izlenir).

## RLS (karar)

- **Tanım tabloları:** okuma `is_active_user()`; **yazma `is_admin_or_owner()`**.
  Gerekçe: bunlar ayarlar alanında (RequireManager) yönetiliyor; Faz 0 settings/roles
  ile tutarlı, geniş-yazma açığı bırakmıyor. Dokümandaki "geniş" ilkesinden bilinçli
  sapma (kullanıcıya bildirildi).
- **`entity_tags`:** iş verisi → `is_active_user()` okur/yazar/siler.

## Frontend

- `src/hooks/useReference.ts` — jenerik CRUD (`useReferenceList/useSaveReference/
  useSetReferenceActive`), `ensureRows` ile sessiz-red koruması, 23505 → anlaşılır mesaj.
- `src/pages/settings/ReferenceListPage.tsx` — jenerik yönetim (renk rozeti,
  is_positive/is_closed sütunları, sistem satırı pasifleştirilemez).
- `src/pages/settings/referenceConfigs.ts` — 6 alt sayfa config'i.
- `SettingsLayout` — gruplu alt-nav: Organizasyon · **Satış Tanımları** · Sistem.
- Rotalar: `/ayarlar/{kanallar,sonuclar,kaynaklar,durumlar,etiketler,musteri-turleri}`.

## Doğrulama

- psql: 6 tablo tohumlu (7/7/8/6/2/6), is_positive/is_closed doğru, RLS açık.
- typecheck/lint/build temiz.
- Görsel: "Potansiyel Durumları" renkli rozetler + Kapanış bayrağı + Satış Tanımları grubu.
