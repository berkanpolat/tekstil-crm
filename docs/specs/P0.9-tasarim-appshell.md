# P0.9 — Tasarım Sistemi ve AppShell · Spec (Ne yapıldı)

## Tasarım token'ları (`src/index.css`)

Marka (navy/orange), yüzey, metin, durum (success/warning/danger/info/neutral —
pastel bg + koyu fg), yarıçap (12/8/6px), gölge (card/pop), tipografi (Inter Variable),
tümü CSS değişkeni + Tailwind 4 `@theme` utility. shadcn semantik token'ları markaya
eşlendi. Odak halkası: turuncu 2px/2px offset.

## Bileşenler

- `src/components/ui/`: shadcn primitive'leri — button, avatar, dropdown-menu, sheet,
  tooltip, separator (`radix-ui` birleşik paketi).
- `src/components/layout/`:
  - `Sidebar.tsx` — daraltılabilir (240↔64px), aktif=turuncu şerit, tooltip (daralınca),
    kullanıcı bloğu (`UserBlock` + çıkış menüsü).
  - `Topbar.tsx` — başlık (aktif rotadan) + mobil hamburger + aksiyon alanı.
  - `AppShell.tsx` — sidebar + topbar + `<Outlet>`; mobilde Sheet çekmece.
- `src/pages/PlaceholderPage.tsx` — modül bilgi kartı (açıklama + gelecek faz).
- `src/hooks/useCurrentUser.ts` — oturumdaki kullanıcı (public.users + departman); yoksa null.
- `src/lib/navigation.ts` — 13 modüllük veri-odaklı menü + `activeNavItem`.

## Yönlendirme

`src/App.tsx` — AppShell layout altında 13 rota + catch-all, hepsi PlaceholderPage.
`main.tsx` — Inter font import + `TooltipProvider`.

## Faz 0 menüsü (13)

Gösterge Paneli · Potansiyeller · Müşteriler · Talepler · Teklifler · Numuneler ·
Siparişler · Katalog · Belgeler · Finans · Görevler · Raporlar · Ayarlar.

## Doğrulama

- typecheck / lint / build temiz (build: chunk >500KB uyarısı — Inter+radix, kabul; ileride code-split).
- `tests/unit/navigation.test.ts` — 5 test (menü, benzersizlik, activeNavItem önek/tam eşleşme).
- Görsel: masaüstü + mobil ekran görüntüsü alındı; aktif şerit, daraltma, mobil çekmece
  çalışıyor.

## Devir

Auth guard + gerçek sayfalar P0.10 sonrası. Topbar aksiyon alanı PageHeader (P0.10) ile dolacak.
