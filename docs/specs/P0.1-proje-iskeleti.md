# P0.1 — Proje İskeleti · Spec (Ne yapıldı)

## Kurulan yığın

| Katman | Paket | Sürüm |
|---|---|---|
| Derleyici | Vite | 8.1.5 |
| UI | React / React DOM | 19.2.8 |
| Dil | TypeScript (strict) | 5.9.3 |
| Stil | Tailwind CSS + @tailwindcss/vite | 4.3.3 |
| Bileşen | shadcn/ui (new-york) + @radix-ui/react-slot | — |
| İkon | lucide-react | 1.26.0 |
| Bildirim | sonner | 2.0.7 |
| Veri | @tanstack/react-query | 5.101.4 |
| Yönlendirme | react-router-dom | 7.18.1 |
| Backend SDK | @supabase/supabase-js | 2.110.8 |
| Tarih | date-fns | 4.4.0 |
| Birim/Entegrasyon test | Vitest + Testing Library + jsdom | 4.1.10 |
| E2E test | @playwright/test | 1.62.0 |
| Lint/Format | ESLint 10 (flat) + Prettier 3 | — |

## Klasör yapısı

```
src/
  pages/            (yer tutucu)
  components/
    ui/             button.tsx
    layout/         (yer tutucu)
    shared/         (yer tutucu)
  hooks/            (yer tutucu)
  lib/              utils.ts, env.ts, supabase.ts, queryClient.ts, database.types.ts
  App.tsx, main.tsx, index.css, vite-env.d.ts
supabase/
  migrations/       (yer tutucu)
  functions/        (yer tutucu)
tests/
  unit/             utils.test.ts
  integration/      (yer tutucu)
  e2e/              smoke.spec.ts
  setup.ts
docs/
  plans/            P0.1-...
  specs/            P0.1-...
public/             favicon.svg
scripts/
```

## package.json script'leri

`dev`, `build`, `preview`, `typecheck`, `lint`, `lint:fix`, `format`,
`format:check`, `test`, `test:watch`, `test:ui`, `test:e2e`,
`db:link`, `db:push`, `db:reset`, `db:diff`.

## Yapılandırma dosyaları

- `tsconfig.json` (çözüm) → `tsconfig.app.json` (tarayıcı, strict+) + `tsconfig.node.json` (Node)
- `vite.config.ts` — react + tailwind eklentileri, `@` → `src` alias, Vitest test bloğu
- `eslint.config.js` — flat config, typescript-eslint + react-hooks + react-refresh, prettier kapanışı
- `.prettierrc.json`, `.prettierignore`
- `playwright.config.ts` — chromium + mobile (Pixel 7) projeleri, webServer=`pnpm dev`
- `components.json` — shadcn new-york, slate taban, CSS değişkenleri
- `.env.example` — VITE_ önekli public değerler + CLI/gizli değerler açıklamalı
- `.gitignore` — `.env*` hariç `.env.example`

## Doğrulama (bu paket için)

- `pnpm typecheck` — hatasız
- `pnpm lint` — hatasız (0 uyarı)
- `pnpm test` — 3/3 geçti (`cn` yardımcısı)
- `pnpm build` — başarılı (dist üretildi)
- `pnpm dev` — HTTP 200, açılış ekranı render ediliyor

## Notlar / sonraki paketlere devir

- `src/lib/database.types.ts` şu an boş yer tutucu; şema kuruldukça
  `supabase gen types` ile üretilecek.
- Tam marka token seti (`index.css`) P0.9'da işlenecek; şu an temel tema var.
- Playwright tarayıcıları henüz indirilmedi (`pnpm exec playwright install`
  E2E ilk çalıştırmadan önce gerekli).
