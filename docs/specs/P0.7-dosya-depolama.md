# P0.7 — Dosya Depolama · Spec (Ne yapıldı)

## Migration

`supabase/migrations/20260725170000_p0_7_files_storage.sql`

## Storage

- Bucket'lar: `documents`, `avatars` — ikisi de `public=false` (imzalı URL erişimi).
- `storage.objects` RLS (yalnızca bu iki bucket, aktif kullanıcı): select/insert/update
  geniş; **delete KOŞULLU** — `owner = auth.uid()` ve `not file_record_exists(bucket,name)`.
- `file_record_exists(text,text)` — SECURITY DEFINER yardımcı (aktif files kaydı var mı).

## files tablosu

Sütunlar: `id, bucket, storage_path, original_name, mime_type, size_bytes, checksum,
category(file_category), entity_type, entity_id, version, replaces_file_id,
description, uploaded_by, deleted_at, deleted_by, created_at`.
İndeksler: unique `(bucket, storage_path)`, `(entity_type, entity_id)`, checksum,
category, aktif (deleted_at is null).

- RLS: select/insert/update aktif kullanıcı; fiziksel delete yok (soft-delete).
- Audit trigger bağlı.
- `users.avatar_file_id → files(id)` FK eklendi (P0.4'ten ertelenmişti).

## Frontend

- `src/lib/database.types.ts` — `supabase gen types` ile üretildi (tüm tablolar + fonksiyonlar).
- `src/hooks/useFiles.ts`: `useUploadFile` (checksum+sürüm+uploaded_by), `useEntityFiles`,
  `getSignedUrl`/`useSignedUrl`, `useDeleteFile` (mantıksal), `isPreviewable`.
- `src/components/shared/FileUpload.tsx`: `FileUpload` (drag-drop) + `FileList`
  (indir/sil/önizleme). İşlevsel; cila P0.10.

## Doğrulama

- psql: bucket'lar private, files RLS açık, avatar FK, 4 storage politikası, helper fn.
- `scripts/e2e_storage.mjs` (10 PASS): upload → files kaydı → imzalı URL → **indirilen
  içerik doğru (#11)**; files kaydı olan nesne **silinemedi**; yarım (kayıtsız) nesne
  **silindi**; anon imzasız erişemedi; temizlik.
- `pnpm typecheck / lint / test / build` — hepsi temiz.

## Not

Storage nesneleri psql'den doğrudan silinemez (`storage.protect_delete`); temizlik
Storage API ile yapılır. Owner ilk kurulumu: [ilk-kurulum-owner.md](ilk-kurulum-owner.md).
